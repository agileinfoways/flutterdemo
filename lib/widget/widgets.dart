import 'package:flutter/material.dart';



/*class SplashImage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
     var assetsImage=new AssetImage('assets/new_logo.png');
     var image=new Image(image: assetsImage);
     return new Container(child: image);
  }

}*/

/**
 *  here can create common widget .
 *  and also can use multiple time
 */

class ListViewButton extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Container(child: new Text("Listview With Api"));
  }

}
